// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: jobs.sql

package db

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const completeJob = `-- name: CompleteJob :exec
UPDATE jobs
SET status = 'succeeded', lease_owner = NULL, lease_expires_at = NULL,
    finished_at = now(), last_error = NULL
WHERE id = $1 AND lease_owner = $2
`

type CompleteJobParams struct {
	ID         string
	LeaseOwner pgtype.Text
}

func (q *Queries) CompleteJob(ctx context.Context, arg CompleteJobParams) error {
	_, err := q.db.Exec(ctx, completeJob, arg.ID, arg.LeaseOwner)
	return err
}

const enqueueJob = `-- name: EnqueueJob :one
INSERT INTO jobs (id, instance_id, kind, payload, created_by)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, instance_id, kind, payload, status, attempts, max_attempts,
          lease_owner, lease_expires_at, last_error, created_by, created_at,
          started_at, finished_at
`

type EnqueueJobParams struct {
	ID         string
	InstanceID pgtype.Text
	Kind       string
	Payload    []byte
	CreatedBy  pgtype.Text
}

func (q *Queries) EnqueueJob(ctx context.Context, arg EnqueueJobParams) (Job, error) {
	row := q.db.QueryRow(ctx, enqueueJob,
		arg.ID,
		arg.InstanceID,
		arg.Kind,
		arg.Payload,
		arg.CreatedBy,
	)
	var i Job
	err := row.Scan(
		&i.ID,
		&i.InstanceID,
		&i.Kind,
		&i.Payload,
		&i.Status,
		&i.Attempts,
		&i.MaxAttempts,
		&i.LeaseOwner,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}

const failJob = `-- name: FailJob :exec
UPDATE jobs
SET status = CASE WHEN attempts >= max_attempts THEN 'failed' ELSE 'queued' END,
    lease_owner = NULL, lease_expires_at = NULL, last_error = $3,
    finished_at = CASE WHEN attempts >= max_attempts THEN now() ELSE NULL END
WHERE id = $1 AND lease_owner = $2
`

type FailJobParams struct {
	ID         string
	LeaseOwner pgtype.Text
	LastError  pgtype.Text
}

func (q *Queries) FailJob(ctx context.Context, arg FailJobParams) error {
	_, err := q.db.Exec(ctx, failJob, arg.ID, arg.LeaseOwner, arg.LastError)
	return err
}

const leaseNextJob = `-- name: LeaseNextJob :one
UPDATE jobs
SET status = 'leased', lease_owner = $1, lease_expires_at = now() + $2::interval,
    attempts = attempts + 1, started_at = COALESCE(started_at, now())
WHERE id = (
    SELECT id FROM jobs
    WHERE (status = 'queued' OR (status IN ('leased', 'running') AND lease_expires_at < now()))
      AND attempts < max_attempts
    ORDER BY created_at
    FOR UPDATE SKIP LOCKED
    LIMIT 1
)
RETURNING id, instance_id, kind, payload, status, attempts, max_attempts,
          lease_owner, lease_expires_at, last_error, created_by, created_at,
          started_at, finished_at
`

type LeaseNextJobParams struct {
	LeaseOwner pgtype.Text
	Column2    pgtype.Interval
}

func (q *Queries) LeaseNextJob(ctx context.Context, arg LeaseNextJobParams) (Job, error) {
	row := q.db.QueryRow(ctx, leaseNextJob, arg.LeaseOwner, arg.Column2)
	var i Job
	err := row.Scan(
		&i.ID,
		&i.InstanceID,
		&i.Kind,
		&i.Payload,
		&i.Status,
		&i.Attempts,
		&i.MaxAttempts,
		&i.LeaseOwner,
		&i.LeaseExpiresAt,
		&i.LastError,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.StartedAt,
		&i.FinishedAt,
	)
	return i, err
}
