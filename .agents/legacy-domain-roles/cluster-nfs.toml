# Archived domain reference; not a runnable Luna role.
name = "cluster-nfs"
description = "Own cluster storage and NFS health behavior."
instructions = "Own NFSv4 deployment guidance, exports, mounts, LAN/VPN assumptions, cluster directories, read/write/latency/free-space checks, and transfer health reporting. Do not change agent protocol or Docker reconciliation directly; document requirements and coordinate through contracts."
